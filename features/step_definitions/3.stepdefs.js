const assert = require('assert');
const { Before, Given, When, Then} = require('@cucumber/cucumber');

Given("As a case manager I am logged in to the case management system", function() {
  // Code to log in as a case manager goes here
});

When("I access the appointment calendar", function() {
  // Code to access the appointment calendar goes here
});

Then("I should be able to view the list of appointments for the current day", function() {
  // Code to check that the current day's appointments are displayed goes here
});